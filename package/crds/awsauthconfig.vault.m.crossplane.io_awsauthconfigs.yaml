---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: awsauthconfigs.awsauthconfig.vault.m.crossplane.io
spec:
  group: awsauthconfig.vault.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - vault
    kind: AWSAuthConfig
    listKind: AWSAuthConfigList
    plural: awsauthconfigs
    singular: awsauthconfig
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                properties:
                  accessIdentity:
                    description: |-
                      AccessIdentity is the identifier for the identity used to sign requests.
                      This is useful when multiple AWS accounts need to authenticate against a single Vault cluster.
                    type: string
                  backend:
                    description: Backend is the mount path of the AWS auth method
                      (e.g. "aws-auth").
                    type: string
                  iamRequestPayload:
                    description: |-
                      IAMRequestPayload is the payload to use for the IAM auth request.
                      Only used if the IAM auth method requires a custom payload format.
                    type: string
                  iamRequestUrl:
                    description: |-
                      IAMRequestURL is the base URL to use for the iam_server_id_header_value request.
                      This is needed for some cloud providers where the AWS STS endpoint is not available.
                    type: string
                  iamServerIdHeaderValue:
                    description: |-
                      IAMServerIDHeaderValue is the value to require in the X-Vault-AWS-IAM-Server-ID header.
                      This provides defense against replay attacks.
                    type: string
                  maxTtl:
                    description: MaxTTL is the maximum duration a token issued by
                      this method is valid (e.g. "24h").
                    type: string
                  stsDisableRedirect:
                    description: STSDisableRedirect disables the redirect behavior
                      when calling STS.
                    type: boolean
                  stsEndpoint:
                    description: |-
                      STSEndpoint is a custom endpoint URL for the AWS STS service.
                      This is useful in environments where the default STS endpoint is not available.
                    type: string
                  stsFallbackRegion:
                    description: STSFallbackRegion is the AWS region to use if the
                      request's region cannot be determined.
                    type: string
                required:
                - backend
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                properties:
                  backend:
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
